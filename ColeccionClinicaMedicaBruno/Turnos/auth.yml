info:
  name: auth
  type: http

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/login
  body:
    type: json
    data: '{ "email": "lopmar@correo.com", "contrasenia": "lopmar" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
