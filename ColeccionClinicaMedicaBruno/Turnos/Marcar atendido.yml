info:
  name: Marcar atendido
  type: http
  seq: 1

http:
  method: PUT
  url: 'http://localhost:3000/api/v1/turnos/1/atender'
  auth:
    type: bearer
    token: ''

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
