info:
  name: especialidades id
  type: http

http:
  method: GET
  url: http://localhost:3000/especialidades/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
