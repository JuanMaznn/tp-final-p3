info:
  name: especiladades
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/especialidades
  body:
    type: json
    data: |-
      {
        "nombre": "ALERGIA E INMUNOLOGÍA"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
